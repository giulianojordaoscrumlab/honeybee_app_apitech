class RecoveryPasswordRepository {
  Future processSendEmail() async {
    //TESTE
    await Future.delayed(
      const Duration(seconds: 2),
    );
  }

  Future processValidateCode() async {
    //TESTE
    await Future.delayed(
      const Duration(seconds: 2),
    );
  }

  Future processChangePassword() async {
    //TESTE
    await Future.delayed(
      const Duration(seconds: 2),
    );
  }
}
