enum PlanType { free, bronze, prata, ouro }

enum AcceptedTerms { read, accepted, denied, wait, none }

enum CadastroState { done, initialized, none, loading }

enum CadastroExpressAddressField { collapsed, expanded, none, loading }

enum ModelStates { loading, idle, completed, error }
